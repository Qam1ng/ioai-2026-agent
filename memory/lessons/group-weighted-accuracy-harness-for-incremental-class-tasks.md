For continual-learning style tasks scored as 0.5*Acc_groupA + 0.5*Acc_groupB, the metric is NOT pooled accuracy — always self-test with an asymmetric case (e.g. 100 A-correct + 10 B-wrong must give 0.5, not 0.91). When building shared folds with classes as rare as 3 samples: StratifiedKFold k=4 still keeps >=2 train samples per class (3-sample class splits 1/1/1/0); assert per-fold train-side min class count >=2 and accept that rare classes have one empty-validation fold — harmless when the metric averages over groups, not classes. Sort rows by a stable key (path) before splitting so folds.csv is byte-identical across regenerations; verify with md5.